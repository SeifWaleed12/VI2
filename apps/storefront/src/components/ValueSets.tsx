"use client";

// ValueSets requires bundles configured in Medusa. 
// Hidden until bundles are created in backend to prevent rendering mock products.
export default function ValueSets() {
  return null;
}
